// Practice Question

// Print all the Even Numbers from 0 to 100.

for (let num = 0; num <= 100; num++) {
    // console.log(num);
    // Below is the condition to print even numbers
    // if (num % 2 === 0) {
    //     console.log("num =", num);
    // }

    //Below is the condition to print odd numbers
    if (num % 2 !== 0) {
        console.log("num =", num);
    }
}